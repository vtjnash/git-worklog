# Your state. refresh.py READS this and never writes it, so notes and snoozes
# here are safe from any refresh. The /dash skill appends and edits entries.
#
# Keys are the item URL. Every field is optional:
#
#   bucket     = "needs-agents"     # force a lane; otherwise derived from facts
#   snooze     = "on-change"        # hide until the PR actually moves
#   snooze     = "2026-09-15"       # or hide until a date
#   deadline   = "2026-09-30"       # your own deadline (milestone due dates come from GitHub)
#   note       = "one line: what the next action actually is"
#   blocked_on = ["JuliaLang/julia#62396"]
#   agent_task = "rebase and rerun Compiler tests"   # promotes into needs-agents

["https://github.com/JuliaLang/julia/pull/62452"]
note = "rebase onto master; verify.jl still bans bare GlobalRef in value position"
deadline = "2026-09-30"

["https://github.com/JuliaLang/julia/pull/62841"]
agent_task = "bisect the llvm-23 CI failures, prepare fixup commits"

["https://github.com/libuv/libuv/pull/5212"]
snooze = "on-change"
["https://github.com/JuliaLang/julia/pull/62396"]
note = "unblocks 62452; rebase this first"
