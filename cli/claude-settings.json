{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "f=/proc/$PPID/fd/1; [ -c \"$f\" ] || f=/dev/$(ps -o tty= -p \"$PPID\" | tr -d ' '); [ -c \"$f\" ] && printf '\\a' > \"$f\"; exit 0"
          }
        ]
      }
    ],
    "Notification": [
      {
        "matcher": "permission_prompt",
        "hooks": [
          {
            "type": "command",
            "command": "f=/proc/$PPID/fd/1; [ -c \"$f\" ] || f=/dev/$(ps -o tty= -p \"$PPID\" | tr -d ' '); [ -c \"$f\" ] && printf '\\a' > \"$f\"; exit 0"
          }
        ]
      }
    ]
  }
}
