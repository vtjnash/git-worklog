__pycache__/
# Machine caches, regenerated by refresh.py. facts.json is ~1MB and churns on
# every run; committing it would bury the diffs that matter in DASHBOARD.md.
facts.json
bulk.json
queue.json
read.json
