# Machine caches, regenerated by `wl refresh`. facts.json is ~1MB and churns on
# every run; committing it would bury the diffs that matter in DASHBOARD.md.
facts.json
bulk.json
queue.json
read.json
touched.json
cache/
repos.toml

# Uncaught errors, appended by the browser. Delete it to clear the warning.
errors.log
